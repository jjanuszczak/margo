## Speaker script

Do not lead with implementation details. Show that Margo keeps the useful parts of a software workflow: small source files, clean review, local preview, and repeatable output.

Markdown and slide-local assets enter the compiler, then a theme shapes the finished deck. HTML is canonical, with PDF, PNG, and PPTX as output formats.

## Release review

When a release changes a command, output format, authoring primitive, or supported theme behavior, review this slide and the product-capability slide before publishing the tagged release.
