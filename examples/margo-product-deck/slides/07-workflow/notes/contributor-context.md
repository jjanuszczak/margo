## Contributor context

The core loop remains the product test: `new` or `init`, author a slide bundle, preview with `serve`, then build HTML. A feature that complicates this loop needs a clear product reason.

Check the PRD before adding broader publishing, plugin, or multi-deck behavior. Those are deliberately constrained areas.
